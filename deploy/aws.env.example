# Copy to repo root `.env` (gitignored) or set in CI secrets — never commit real secrets.
# Current production values (update if you recreate resources):

AWS_REGION=us-east-1
AWS_DEFAULT_REGION=us-east-1

# S3 bucket for Expo web static export (apps/mobile/dist)
AWS_S3_BUCKET=unicorn-gives-web-391668783184-1774490745

# CloudFront distribution (default *.cloudfront.net; add unicorn.gives in console/CLI after ACM ISSUED)
AWS_CLOUDFRONT_DISTRIBUTION_ID=ER9WIPUKON9J3

# Route 53 hosted zone for unicorn.gives (DNS fully on AWS)
AWS_ROUTE53_HOSTED_ZONE_ID=Z06458021X03LCFPVFU9W

# ACM in us-east-1 (pending until registrar uses Route 53 nameservers)
AWS_ACM_CERTIFICATE_ARN=arn:aws:acm:us-east-1:391668783184:certificate/06b90169-66de-4ebd-96c0-b4a9bea27372

# Expo build-time (GitHub Actions secrets)
# EXPO_PUBLIC_SUPABASE_URL=
# EXPO_PUBLIC_SUPABASE_ANON_KEY=
# EXPO_PUBLIC_NEWSLETTER_URL=
